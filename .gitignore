# Secrets — never commit these.
claude-key.txt
discord-app.json
discord-token.json

# Local state, not code.
notes.txt

# Dependencies and build output (V2 app shell).
node_modules/
v2/dist*/

# Windows / editor noise.
Thumbs.db
desktop.ini
*.log

# Local Claude Code settings, not project config.
.claude/settings.local.json

# ANCS bridge build output (rebuilt with: cd ancs && dotnet publish -c Release -o bin/out)
ancs/bin/
ancs/obj/
