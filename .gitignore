# Secrets — never commit these.
claude-key.txt
discord-app.json
discord-token.json

# Local state, not code.
notes.txt

# Dependencies and build output (V2 app shell).
node_modules/
v2/dist/

# Windows / editor noise.
Thumbs.db
desktop.ini
*.log
